import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Dejarlo vacío es lo más seguro.
  // Astro usará las opciones por defecto que funcionan perfectamente.
});
